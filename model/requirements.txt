# Core modeling stack
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# NFL data: play-by-play with EPA precomputed, schedules, and historical
# betting lines (spread_line, moneylines). Optional -- the pipeline falls back
# to a synthetic dataset if this is missing or offline.
#
# nfl_data_py is the widely-used package; its maintained successor is nflreadpy
# (https://github.com/nflverse/nflreadpy). Either works as the real-data source.
nfl_data_py>=0.3.2

# Testing
pytest>=7.0
