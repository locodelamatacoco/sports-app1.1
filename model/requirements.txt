# Core modeling stack
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Real NFL data (scores + closing lines) is read straight from the nflverse
# schedules CSV with pandas -- no extra package needed. Live odds are scraped
# from OddsTrader using only the standard library.

# Testing
pytest>=7.0
